<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="dmRoom">
	
	<select id="sequence">
		select dm_room_seq.nextval from dual
	</select>
	
	<insert id="create">
		insert into dm_room(room_no, room_name, room_created)
		values(dm_room_seq.nextval, #{roomName}, sysdate)
	</insert>
	
	<!--  채팅방 유무  -->
	<select id="find" resultType="DmRoomDto">
		select * from dm_room where room_no = #{roomNo}	
	</select>
	
	<!-- 채팅방 목록 -->
	<select id="list" resultType="DmRoomDto">
		select * from dm_room order by room_no desc
	</select>
	
	<!-- 채팅방 이름 변경 -->
	<update id="updateRoomName">
        update dm_room set room_name = #{roomName} where room_no = #{roomNo}
    </update>
    
    <!-- 유저가 없을 경우 채팅방 삭제 -->
    <delete id="deleteIfEmpty">
    	delete from dm_room
    	where room_no = #{roomNo}
    	and not exists (
    		select 1 from dm_user
    		where room_no = #{roomNo}
    	)
	</delete>

</mapper>